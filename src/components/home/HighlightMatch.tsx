/**
 * Retrocompat: o componente foi movido para `@/components/search/HighlightMatch`.
 * Mantemos os mesmos exports para não quebrar imports existentes.
 */
export { HighlightMatch, tokenizeQuery } from "@/components/search/HighlightMatch";
