/**
 * Rótulos canônicos de métricas da plataforma.
 *
 * Use estes valores em relatórios, contadores, cards de dashboard e
 * exportações para que a mesma métrica tenha sempre o mesmo nome na
 * interface. O antigo termo "Cotações" foi descontinuado.
 */

/** Contagem de preços coletados (registros da tabela de scans). */
export const PRICE_RECORDS_LABEL = "Registros de preço";

/** Versão curta, para colunas de tabela e espaços estreitos. */
export const PRICE_RECORDS_LABEL_SHORT = "Registros";

/** Texto auxiliar padrão para tooltips/hints da métrica. */
export const PRICE_RECORDS_HINT = "Preços coletados e conferidos";
